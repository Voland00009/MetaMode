# Daily Log: 2026-04-11

## Sessions

### Session: Phase B.2 — SDK→CLI Migration (23:02)

**Context:** Phase B.2 implementation — SDK→CLI migration for 4 core scripts (flush, compile, lint, query)

**Key Exchanges:**
- Estimated all 3 tasks fit in one session despite being "on the edge" — confirmed correct
- Discovered Python patching pattern: must patch module-consumer (`flush.DAILY_DIR`, `utils.DAILY_DIR`, `lint.KNOWLEDGE_DIR`), not `config.*`, because Python caches import-time references

**Decisions Made:**
- Removed `max_turns` from all `run_claude_prompt` calls (consistent with B.1 finding that flag doesn't exist)
- Patching strategy: always patch at the consuming module level, not at `config` — this became the standard pattern across all test files

**Lessons Learned:**
- Python import-time binding gotcha: `from config import X` copies the reference at import time, so `unittest.mock.patch('config.X')` doesn't affect code that already imported it. Must patch `module_under_test.X` instead
- TDD RED phase caught this early — tests failed for wrong reasons (import errors), fixed before writing production code

**Action Items:**
- Phase B.3 next: Tasks 6-9 (!save hook, AGENTS.md, session-start.py, /reflect + /compile skills)

### Session: Phase B.4 — Integration Testing (23:26)

**Context:** Phase B.4 — Integration testing and smoke test of all MetaMode components

**Key Exchanges:**
- Ran all 29 unit tests — passed
- Smoke tested each component: compile, lint, session_start, user_prompt_submit, query, skills, hooks
- Found critical bug in claude_cli.py causing garbled output in pending-review.md

**Decisions Made:**
- Cleaned pending-review.md of corrupted data (character-per-line garbage) — data was unrecoverable, replaced with clean template

**Lessons Learned:**
- `claude -p --output-format json` can return `{"result": "text"}` (string) instead of `{"result": [{...}]}` (array). Iterating a Python string yields individual characters — each passes `isinstance(block, str)`. Fix: check if `result` is a string before iterating, return it directly
- This is the same class of bug as the Phase B.2 import-time binding gotcha — Python's duck typing lets structurally wrong data silently pass type checks

**Action Items:**
- Phase B complete (B.1-B.4). MetaMode v1 ready for real use
- Untested with real `claude -p`: query.py live queries, compile.py without --dry-run, full end-to-end session cycle

## Memory Maintenance

### Memory Flush (23:51)

FLUSH_ERROR: RuntimeError: claude -p failed (exit 1): 

